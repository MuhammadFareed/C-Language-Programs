#include<stdio.h>
#include<conio.h>
void table(void);
int main()
{
	table(n);
	getch();
}
void table(int n)
{
	int n,j,ans;
	printf("Enter the number whose table you want to print : ");
	scanf("%d",&n);
	for(j=1;j<=10;j++)
	{
		ans=n*j;
		printf("\n%d * %d = %d\n",n,j,ans);
	}
}
