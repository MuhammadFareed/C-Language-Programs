#include<stdio.h>
#include<conio.h>
int table(void);
int main()
{
	table();
	for(j=1;j<=10;j++)
	{
		printf("\n%d * %d = %d\n",n,j,ans);
	}
	getch();
}
int table(void)
{
	int n,j,ans;
	printf("Enter the number whose table you want to print : ");
	scanf("%d",&n);
	for(j=1;j<=10;j++)
	{
		return(ans,n,j)
	}
}
